/**
 * Next.js Configuration File
 * 
 * This file configures Next.js framework settings for the entire application.
 * It controls build behavior, optimization, and runtime features.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable React Strict Mode for highlighting potential problems in the application
  // This activates additional checks and warnings in development mode
  reactStrictMode: true,
}

module.exports = nextConfig
